fun main(args: Array<String>) {
    var varBool: Boolean = true
    if (varBool)
        println("varBool bernilai " + varBool)

    var varInt: Int = 1
    // Memeriksa apakah varInt bukan nol atau memiliki nilai tertentu
    if (varInt != 0)
        println("varInt bernilai " + varInt)
}
